#include"main.h"
/**
 * _abs - main entry blocks
 * description : compute an absolute value for an intiger
 * @n : an interger to be take as an input
 * Return: absolute of n
 */
int _abs(int n)
{
	return (n < 0 ? n * -1 : n);
}
