#include"main.h"
/**
 * _islower - main entry blocks
 * description : checks a lowercase letter
 * Return: 0
 */
int main(void)
{
	int c;

	if ( _islower(c))
	{
		retur (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
	return (0);
}
